{{#each posts as |post| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      {{{get_emoji}}}
      <a href="/post/{{id}}">{{title}}</a>
    </h2>
    <p>Posted by {{user.name}} on {{format_date createdAt}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{contents}}
    </p>
  </div>
</div>
{{/each}}
